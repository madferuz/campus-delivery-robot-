# pytest.ini
# Registers the custom marks used in delivery_tests so pytest stops
# warning about them.

[pytest]
markers =
    needs_ros: integration tests that require a live ROS master (use --with-ros to run)

testpaths =
    src/delivery_tests/tests

filterwarnings =
    ignore::DeprecationWarning
